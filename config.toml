# Forces the light theme so the app does not follow a viewer's dark-mode
# setting, which made labels, tab names and buttons unreadable.
[theme]
base = "light"
primaryColor = "#0E8074"
backgroundColor = "#EDF1F5"
secondaryBackgroundColor = "#FFFFFF"
textColor = "#1B2A38"
font = "sans serif"
